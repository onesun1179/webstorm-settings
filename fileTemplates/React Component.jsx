import { memo } from "react"
import PropTypes from 'prop-types';

const ${NAME} = memo(() => {
    return null
})

export default ${NAME}

${NAME}.propTypes = {}
${NAME}.defaultProps = {}
${NAME}.displayName = "${NAME}"